import React from 'react';
import OnboardingForm from 'web/components/OnboardingForm';

const Onboarding = ({ location, history }) => {
  return <OnboardingForm location={location} history={history} />;
};

export default Onboarding;
