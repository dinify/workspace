import reducer from './reducers';

export { default as translationSelectors } from './selectors';
export { default as translationEpics } from './epics';
export { default as translationTypes } from './types';

export default reducer;
