import reducer from './reducers';

export { default as restaurantSelectors } from './selectors';
export { default as restaurantEpics } from './epics';
export { default as restaurantTypes } from './types';

export default reducer;
